<li>
	{{name}}

	<button class="btn btn-primary devour" data-id="{{id}}" data-eaten="{{eaten}}">
		{{#if eaten}}Devour!{{else}}Re-Make{{/if}}
	</button>

	<button class="btn btn-primary delete" data-id="{{this.id}}">DELETE!</button>
</li>
